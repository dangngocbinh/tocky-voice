/** The one constant the update flow needs on every platform and every failure path. */

/** Manual-download fallback. A constant, never taken from the (signed but still
 *  untrusted-for-navigation) update manifest. */
export const RELEASES_URL =
  "https://github.com/dangngocbinh/tocky-voice/releases/latest";
