# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Credentials must never enter the repo — the app keeps them in the OS keychain,
# but a stray dotenv from local experiments would be committed without this.
.env
.env.*
*.pem
*.key

# Tauri generated
src-tauri/gen/

# Rust compile output — several GB, and regenerated by any compile
src-tauri/target/

# Internal planning notes and agent reports: working material, not the product
plans/
